<?xml version="1.0" encoding="UTF-8"?>
<!--
  Log4j 2 configuration for jades-examples unit tests.
  All simulation output is silenced during test runs to keep the Maven
  Surefire report clean.  Set the system property "test.log.level" to
  DEBUG or TRACE to enable verbose output:

      mvn test -Dtest.log.level=DEBUG
-->
<Configuration status="WARN">
    <Properties>
        <Property name="ROOT_LEVEL">${sys:test.log.level:-OFF}</Property>
        <Property name="PATTERN">%d{HH:mm:ss.SSS} %-5level %logger{40} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Simulation engine: silenced by default, use -Dtest.log.level=DEBUG to see events -->
        <Logger name="es.ull.simulation" level="${ROOT_LEVEL}" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <!-- Catch-all: also silenced during tests -->
        <Root level="${ROOT_LEVEL}">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
